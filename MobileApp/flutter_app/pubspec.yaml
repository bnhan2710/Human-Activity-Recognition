name: flutter_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.32.0
  firebase_auth: ^4.16.0
  firebase_database: ^10.5.7
  cloud_firestore: ^4.17.5
  sensors_plus: ^3.0.2
  intl: 0.20.2
  fl_chart: ^0.65.0
  permission_handler: ^11.0.1
  # flutter_bluetooth_serial: ^0.4.0  # Tạm thời tắt để build Android
  # flutter_libserialport: ^0.4.0     # Tạm thời tắt để build Android

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true